2.5.7 历史账单本地缓存稳定版

- 以 15.zip 为唯一基准。
- 修复 safe-state.js 中重复残留代码造成的模块语法错误，该错误会导致首页显示 0桌/0桌。
- 首页、计时、预约启动不再触发完整历史账单下载。
- 只有进入收银记录页面时才分批下载全部 Firestore records。
- 显示总数、已读取数量和百分比。
- 每批立即写入 IndexedDB，失败或关闭后可从上次文档继续。
- 完成后永久使用本机完整缓存，仅监听新增账单。
- Firestore 持久化缓存初始化失败时自动降级，不再阻塞整个程序。
