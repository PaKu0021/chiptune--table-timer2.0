Chiptune Table Timer 2.7.2

修复：
- 在计时器中创建、拆分或重组桌位后，同时更新这些桌位已有 records 账单中的 groupId、groupName、groupColor。
- 今日账单会立即按计时器里的统一组重新排列，不再出现“计时器已成组、账单仍未分组/仍在旧组”的情况。
- 从组中移出的桌位，其进行中账单也会同步取消旧组引用。

说明：
- 今日账单的数据源是 records 集合，不是仅根据 shop/main.tables 临时推算。
- 2.7.1 只更新了 tables/groups，导致两个页面显示不一致；2.7.2 已补齐账单同步。
