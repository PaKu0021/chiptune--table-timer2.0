2.5.7 历史账单读取修正版

- 以 16.zip 为唯一基准。
- 修复旧版本错误写入 complete=true 后，本机实际没有完整记录却不再读取云端的问题。
- 启动时用“本机实际条数 + Firestore 总数”校验缓存完整性。
- 发现游标进度和本机记录数量不一致时自动从头重新扫描，并按账单 ID 去重，不会重复。
- 每批改为 5 条，单批超时提高到 90 秒，减少含大尺寸收款截图时反复卡在同一批。
- 读取失败时在页面直接显示具体错误原因，并在 5 秒后继续。
- 完整读取后仍保存到 IndexedDB，之后只同步新增记录。
