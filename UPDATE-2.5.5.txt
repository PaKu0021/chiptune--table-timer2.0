2.5.5 全历史账单本地缓存版

- 首次按10条一批读取 Firestore records 全部历史记录。
- 显示总数、已读取数量和百分比进度。
- 每批读取后立即写入 IndexedDB，本次中断前的数据不会丢。
- 全部读取完成后标记为已缓存，之后启动直接读取本机。
- 后续只监听并合并新产生的账单，不再重复下载全部历史。
- 完整收款截图仅保存在 IndexedDB，localStorage 只保留轻量应急副本。
