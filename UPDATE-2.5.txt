Chiptune Timer 2.5（以 12(1).zip 为唯一基准）

1. 二维码显示页：每秒本地刷新倒计时，每5秒主动从 Firestore 服务器刷新桌位状态；重新联网和回到前台时也会立即刷新。
2. 所有提示音、系统通知和震动永久关闭；超时仅保留页面文字与颜色提示。
3. 收银记录：统一读取 Firestore records 完整集合，并合并本机待上传记录；服务器读取失败时每5秒重试。
4. 多设备同步：在线操作立即上传云端；不再把主 iPad 的 tables 永久覆盖云端。所有设备通过 shop/main 实时监听共享桌位状态。
5. 弱网：操作仍先保存本机，在线时立即同步，且每5秒自动重试待上传项目。
