Chiptune Table Timer v4.0.3 稳定同步修复

主要修改：
1. 开始桌位时只对 tables/table_XX 做事务锁定，不再事务读写整份 shop/main。
2. 桌位锁定增加 10 秒超时，避免界面永久停在“正在由服务器锁定桌位”。
3. 账单文档与桌位锁定在同一事务中提交。
4. 本地状态在锁定成功后更新，后续由现有同步队列兼容更新 shop/main。
5. 全部静态资源缓存版本统一更新为 v4.0.3。

部署：覆盖上传全部文件。首次打开时关闭旧标签页后重新进入。
